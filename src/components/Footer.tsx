function Footer() {
    return (
        <footer className="footer-bar">
            <p>
                &copy; {new Date().getFullYear()} My ToDo App
            </p>
        </footer>
    );
}
export default Footer;
