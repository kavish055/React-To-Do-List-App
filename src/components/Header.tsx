function Header() {
    return (
        <header className="header-bar">
            <img src="/logo.svg" alt="logo" width={36} height={36} className="logo-img" />
            <h1 className="header-title">ToDo List</h1>
        </header>
    );
}
export default Header;
